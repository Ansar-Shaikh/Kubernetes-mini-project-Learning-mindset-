apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-app-test
spec:
  replicas: 2  # Number of pods
  selector:
    matchLabels:
      app: kubernetes-app-test
  template:
    metadata:
      labels:
        app: kubernetes-app-test
    spec:
      containers:
      - name: kubernetes-app-test
        image: kubernetes-app-test:latest
        imagePullPolicy: Never  # Tell Kubernetes not to pull from a registry
        # image: vikash95/kubernetes-test-app:latest
        resources:
          limits:
            memory: "64Mi"
            cpu: "200m"
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: kubernetes-app-test
spec:
  selector:
    app: kubernetes-app-test
  ports:
  - port: 8080
    targetPort: 5000